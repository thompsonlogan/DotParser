digraph G {{ ->
    main -> compare -> execute;
    main -> init;main -> cleanup;
    execute -> makeString;
    execute -> printf;
    init -> makeString;
    main -> printf;
    execute -> compare;
}